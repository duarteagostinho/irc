|-----------------------------------------------------------------|

- Added fcntl to the socket before binding
REASON: It's necessary to put the socket into non-blocking mode

|-----------------------------------------------------------------|

- Changed accept on new connection from:

int user_fd = accept(_sockfd, NULL, NULL);

int user_fd = accept(_sockfd, (struct sockaddr *)&user_socket, &user_size);

REASON: Can use the parameters to save information from the users

|-----------------------------------------------------------------|

- Changed User Class:
private:
	int 				_fd;
	std::string 		_nickname;
	std::string 		_username;
	struct sockaddr_in	_address;
	int 				_addr_size;

	std::string			_active_channel; // Not sure if necessary

REASON: Has the new relevant information that we are saving

|-----------------------------------------------------------------|

- Increased buffer to 4096
REASON: I believe it would be harder to break, although it seems IRC protocol maximum message len is 512 ? (510 + "\r\n")

|-----------------------------------------------------------------|

- Finished Channel Constructors and = Operator
REASON: Thank you for the bug Andrade LOL

|-----------------------------------------------------------------|